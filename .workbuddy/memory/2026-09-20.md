# 2026-09-20 工作日志

## 白雾真根因（用户 13:06 关键线索，下次续修）
- 用户澄清：白雾出现在「左边岛右边弹一次『正在跳转』弹窗之后」，以及「文章面板展开时」。
- 截图2：右岛里多出一个白圆圈「Ⅱ」（暂停/加载图标）——疑似 PageLoader/导航加载
  toast 把自己停在了岛上、或顶掉了岛内按钮，同时岛的玻璃没了（白雾）。
- 排查方向（下次从这里开始）：
  1. PageLoader（src/components/features/PageLoader.astro）「正在跳转」弹窗定位在
     岛边、结束后是否 revert/挤压了 #navbar > div 的内联玻璃（引擎 skip 不重试）；
  2. 文章展开 → ai-island-split → guardNavbarSplit revertGlass(frame) 的路径是否误伤左岛；
  3. swup 换页后 body class 渐变（wallpaper-transparent 等）时引擎重新注册的时序。
- 已完成（本日）：ProMax 岛鼠标移开自动隐藏（MainGridLayout 新 inline 脚本，
  钉住=箭头、搜索展开/拖拽/焦点例外、触屏不生效）；岛面玻璃质感层
  （promax.css 1c 节：斜切受光+白棱+投影，亮暗两套）；isNavbarDockedBottom
  1024→768 对齐；箭头初始图标归位（resetFullscreenNavbarState 没人调用的坑）；
  ProMax 下摘掉箭头 inline onclick（模块脚本 deferred 引发的 ReferenceError 竞态）。
- 全部断言通过：自动隐藏 9 步流程、normal 版不受影响、暗色/亮色截图 OK、tsc 干净。

## ⚠ 关机约定
- 用户要求 13:10 自动关机，但 shutdown 命令被沙箱拦截且用户拒绝了提权 → **没有执行**，
  电脑不会自动关。用户如需关机请自己在终端跑 `shutdown /s /t 60`。
- 未修完的白雾问题留给下次。

# 2026-09-25 工作日志

## 第四轮：调参面板去掉持久化（2026-09-25 上午）
用户截图调参面板常驻页面。根因：`TopSearch.svelte` 的 onMount 里
`sessionStorage.getItem(TUNE_KEY) === "1"` 会恢复上次开关状态 —— 用户按过一次
Ctrl+Shift+E 后，面板每次刷新都自己出现。修法：删掉 TUNE_KEY 读写与常量，
每次打开页面都从关着开始，仅靠 Ctrl+Shift+E 切换。
探针验证（并预置 sessionStorage 种子确认旧值不再被读）：fresh=false →
按键=true → 再按=false，无 JS 错误，tsc/biome 干净。
教训：给"调试工具"做状态持久化是反模式 —— 调试 UI 应当永远默认关闭。

## 收尾：ProMax 两个需求（承接 09-24 未完成的部分）

### ① 「正在跳转」搬进右侧动作岛
- 搬移脚本（MainGridLayout）本身已生效：`#media-island` 的父容器是
  `#navbar-actions .navbar-actions-inner`，`inActions: true`。
- 本轮修掉两个问题：
  - **胶囊被压扁成「正…」**：沉底导航那条横向 flex 是「刚好占满」的
    （左岛 430 + 间隙 12 + 右岛 250 = 容器 692），`.mi-slot` 又是
    `overflow:hidden; min-width:0`，于是胶囊被当可压缩项。修法见
    `src/styles/promax.css` 1d 节：给 `#media-island` / `.mi-pill`
    `flex: 0 0 auto`、给 `.mi-slot` `overflow:visible; min-width:max-content`
    —— 行宽不够就让整条导航岛变宽（居中，左右一起长）。现在 89px 完整显示。

### ② 文章在岛内展开（仅 ProMax）
- 交互：面板根列表点「归档/分类/标签…」→ 不改页，面板原地换成该页的链接清单
  +「‹ 返回」；清单条目是真 `<a>`，点了才走 swup 跳转。清单来源是
  `fetchSectionLinks()`（拉目标页 HTML、抽 `main` 里的站内链接，最多 20 条）。
- 本轮定位并修掉 6 个真 bug（都在 `src/components/layout/ArticleIsland.astro`，
  外加 MediaIsland / MainGridLayout / promax.css各一处）：
  1. **点了卡片还是整页跳转** → swup 的链接拦截挂在 `document` **捕获阶段**，
     只检查 `e.defaultPrevented`，跑在 `<a>` 的处理器之前，所以 preventDefault 无效。
     修法：面板展开时给根卡片挂 `data-no-swup`（`armBrowse()`），收起时
     `resetPanel` 摘掉。swup 见标记直接放过，导航闸门只剩我们的 preventDefault。
  2. **「正在跳转…」仍会亮** → `MediaIsland.astro` 那条监听也是 `capture: true`
     且白检查 `defaultPrevented`。加了 `if (link.hasAttribute("data-no-swup")) return;`。
     另 `MainGridLayout` 的 `startLoading` 监听也补了 `if (e.defaultPrevented) return;`。
  3. **清单永远是空的（「这一页没有可列的链接」）** → `new URL(raw, pageUrl)` 里
     `pageUrl` 是卡片 href（相对路径 `/archive/`），**相对路径不能当基准，直接抛错**，
     每条链接都被 `continue` 掉。修法：`const base = new URL(pageUrl, location.href)`。
  4. **点「返回」后面板失展** → `measureExpandedHeight()` 为了量高度临时
     `add("no-anim","is-expanded")`，量完**无条件 remove("is-expanded")**；
     而 `exitBrowse` 正好调它，把已展开的面板打回未展开态。改成「无损测量」
     （只还原成调用前的样子，`no-anim` 同理，顺便让返回的高度动画回来了）。
  5. **enterBrowse 跑两遍 / fetch 发两次** → `items` 同时从 `island` 和 `panel`
     查 `[data-ai-item]`，而面板就在岛的子树里 → 同一张卡片绑两次。
     修法：`new Set([...])` 去重。
  6. `resetPanel` 补上摘 `data-no-swup`（不摘的话收起后再点会变成整页刷新）。

## 验证（全部通过）
- `tmp/prep-browse-verify.js` 21 项断言全绿：媒体岛在右岛内、面板展开、
  卡片已武装、**点击后 URL 未跳转**、浏览层显示、清单 2 条
  （07-31日记手机版UI 在修了 / 07-29日记半成品）、面包屑「归档」、
  返回后仍展开、收起后已解除武装、无「正在跳转」弹窗、清单条目走 swup。
- normal 版回归（`tmp/prep-normal-regress.js`）：卡片不武装、点击照旧跳转到
  `/archive/`、浏览层不出现 —— 没串味。
- `tsc --noEmit` 干净。

## 坑（重要，别再踩）
- **`tsc --noEmit` 不检查 `.astro` 的 `<script>`**：这次把 `(e) => {` 误改成
  `e) => {`（语法错误），tsc 照样 exit 0。改完 `.astro` 脚本要从 dev server
  的模块 URL 验真：抓首页 HTML 里那个 `src`，fetch 回来看 status +
  有没有 `Transform failed`，并 grep 新函数名在不在。
- **同一文件不要并行发多个编辑**：并发编辑会丢改动，每次都报 success，
  只有最后一个存活。这次连丢三次（items 去重、resetPanel 摘标记、删调试残留）。
  → 一次一改、改完立刻 grep 核对。
- **`probe.js` 不转发 `console.*`**：页面侧调试数据要塞 `window.__dbg` 数组、
  在 prep 返回值里读回；抓调用来源就把 `new Error().stack` 存进数组。

## 第二轮：左岛变白的真凶 + ProMax 岛不拉伸

### ① 「切换站内任意一个链接左岛就变白」——根因定位 + 修复
- **定位证据（探针 `tmp/probe-island.js`，swup.navigate('/archive/') 前后对比）**：
  - 切页前 `#navbar > div`：`background-color = oklch(... / 0.198)`（引擎透底后），
    内联 `backdrop-filter = url("#lg-1") brightness(1.04)`，内联 `background-color` 在；
  - 切页后：`background-color = oklch(... / 0.55)`（**样式表原值**）、内联三项**全空**、
    `backdrop-filter = blur(20px)`（样式表原值），而 `data-lg-on` / `__lgOn` **仍然在**；
  - 右岛（`.navbar-actions-inner`）全程 `0.198 + url("#lg-2")` 没变 → 只有左岛中招。
- **真凶**：`ArticleIsland.astro` 的 `clearFrame()`（挂在 `swup:beforeContentReplace`
  上，本来只是「兜底复位」）用 `frame.style.removeProperty("background"/"backdrop-filter"/…)`
  把**引擎写在这元素上的内联 !important 一起抹了**；而引擎账本还在，
  `applyGlass` 一进门 `if (el.__lgOn) return true` 就再也不会重写 →
  岛永久退回 `color-mix(--card-bg 55%)` + `blur(20px)`（用户截图里那块白雾板）。
- **修复（三处，都在 `src/`）**：
  1. `LiquidGlass.astro`：`applyGlass` 开头加「验货」`glassInlineIntact(el)` ——
     `__lgOn` 在、但内联 `backdrop-filter`/透底 `background-color` 没了 → `revertGlass`
     后整块重来。**任何组件再把内联值抹掉都能自愈**。
  2. `ArticleIsland.astro`：新增 `engineOwns(el)`（`data-lg-on`）+
     `rebuildEngineGlass(el)`（`__liquidGlass.rebuild`）；`clearFrame` /
     `resetPanel` / `endSplit` 的 rAF 收尾 / `open()` 里那句
     `backdrop-filter:none` 全部改成「引擎在岗就不碰、或抹完立刻喊它重写」。
  3. `measureExpandedHeight()` 改成「还原调用前的内联 width」而不是一律
     `removeProperty("width")`（岛不拉伸时面板宽度是内联的，抹了会塌回 44px）。
- **验证**：基线 / 自愈（手动抹掉内联后 `run()` 恢复）/ 展开 / 收起 / 切页 /
  切页+1.5s —— 六步全部 `0.198 + url("#lg-1") + data-lg-on`；
  normal、lite 版的展开仍是「岛撑到 480 + 三胶囊分裂」（没串味），无 JS 报错。

### ② ProMax 文章展开不再把岛向两边拉伸（`ArticleIsland.astro`）
- `measure()` 新增 `noStretch = isDockBottom()`（ProMax 桌面）：宽度直接取
  `PANEL_TARGET_W`（夹进视口），不撑「文章」那一列；`yOuter`（面板底边压在岛的
  **上沿** `fr.top`）、`leftInCol`（以**导航岛**为中线、夹回视口）。
- `open()`/`close()` 分支：noStretch 时**不做** `island.style.width = g.w`、
  不 `startSplit()`（三胶囊没了）、不 `ai-live`（按钮留着）、不淡出 navbar-pill；
  面板自己写 `left/width` 内联，从 44px 的玻璃条长到 `panelH`。
- 效果：岛宽 430、各导航项 x 坐标在展开前后**逐项不变**（探针断言），
  面板浮在岛上方、左右各冒一小截。
- 验收截图：`.workbuddy/artifacts/pm-open.png`、`pm-nav-open.png`、
  `pm-archive-open.png`、`nav_island_zoom.png`。

## 第三轮：文章面板「做成液态玻璃」（真因：显隐观察者把 nest 目标判出局）

### 症状
- 用户截图：ProMax 展开「文章」面板 = 一块 0.198 白底 + `blur(5px)` 的磨砂板，
  没有折射、没有棱线（原话「做成液态玻璃而且你看图片这是什么」）。
- 探针：`[data-ai-panel]` 展开后 `data-lg-on` **缺失**，`backdrop-filter: blur(5px)`
  （样式表那份）；`--lg-tint-bg` 是从**导航岛继承**来的 0.198（面板渐变里用的就是它）。

### 真因（`src/components/features/LiquidGlass.astro`）
1. 面板 SSR 就是 `visibility:hidden` → `applyGlass` 的可见性检查直接 return false →
   `run()` 给它挂 `watchReveal`（MutationObserver），等露头再上玻璃。
2. 露头时观察者回调 `if (!isReadyTarget(el)) return;`，而 `isReadyTarget` 里有
   `el.closest("[data-lg-on]")` —— **面板就长在已上玻璃的导航岛内部**，于是被自己的
   祖先判出局：观察者既不上玻璃也不自摘，面板永远停在白板。
   （PRESET 里 `.ai-float-island` 明明是 `nest: true`，但 isReadyTarget 没传 cfg 进来。）
3. 第二层坑：手动 `register()` 能上玻璃，但「展开→收起→再展开」会丢一次 ——
   收起时面板尺寸变化 → ResizeObserver → `rebuildNow()` → `revertGlass` + `applyGlass`，
   此刻面板已 `visibility:hidden` → 重上失败，而观察者上次成功后已自摘 → 没人再管。

### 修复（LiquidGlass.astro 三处）
- `isReadyTarget(el, cfg)`：`if (!(cfg && cfg.nest) && el.closest("[data-lg-on]")) return false;`
  （nest 目标允许住在玻璃里）；三个调用点全部传 cfg（⚠ 同一文件并行编辑会丢改动，逐个改完 grep 核对）。
- `applyGlass`：可见性失败时 `if (cfg.force) watchReveal(el, cfg);` —— 点名目标每次显形
  都要有玻璃，观察者自摘后必须重挂。
- 面板底色是**渐变**（`background-color` 恒为 transparent），所以引擎只给它折射、不写
  `background-color`；透底靠继承岛的 `--lg-tint-bg` → 与岛同材质，正是想要的效果。

### 顺手：用户截图里那枚大白「⌄」不是面板的东西
- 是首页壁纸全屏模式的 `#scroll-down-indicator`（向下滚动提示，
  `bottom:5rem; left:50%; z-index:10`）。ProMax 把岛沉底后它正好和岛/面板叠在一起，
  隔着半透玻璃透出来，看着像面板里凭空多了个箭头。
- `promax.css` 新增：`body.site-promax:has(.article-island.open) #scroll-down-indicator
  { opacity: 0; pointer-events: none; }`（面板开着时让位，收起自动回来）。

### 验证
- 七步断言（初始/展开/收起/再展开/切页/切页后展开/切页后收起）：三次展开全部
  `data-lg-on + url("#lg-17") brightness(1.04)`；岛全程 `url("#lg-1")` 没丢；
  展开时指示器 `opacity: 0`、收起恢复 `1`；无 JS 报错。
- normal / lite：面板仍不上玻璃（引擎被版本门挡着），无报错，行为未变。
- biome format、tsc --noEmit 干净；验收截图 `.workbuddy/artifacts/panel-glass-full.png`
  （放大件 `panel-glass-zoom.png`）。

### 坑（本轮新增）
- **探针必须先等 `document.readyState === "complete"` 再点**：dev server 的模块图很慢，
  `PREP_DELAY=4500` 时页面还停在 `interactive`，组件脚本的 DOMContentLoaded 回调没跑 →
  `[data-ai-trigger]` 上**一个监听都没有**（`getEventListeners(trig)` 返回 `{}`），
  `click()` 毫无反应。我因此白查了很久（一度怀疑 open() 被改坏）。
  prep 开头统一加：`for (let i=0;i<100 && document.readyState!=="complete";i++) await sleep(300);`
- 判断「引擎能不能玻璃化某元素」最快的三分法：探针里直接
  `window.__liquidGlass.register(panel, cfg)` —— 返回 true 且 `data-lg-on` 出现
  = 元素没问题，问题在**谁去调它**（观察者/扫描路径）。

### 坑（上一轮）
- **玻璃的内联值只属于引擎**：站在组件侧看，「把内联样式清干净」是很自然的复位写法，
  但 `background-color` / `backdrop-filter` 正是引擎的地盘。判据永远是
  `el.hasAttribute("data-lg-on")`；要抹就先抹再 `__liquidGlass.rebuild(el)`。
- **`window.__liquidGlass.stats()` 返回的 `applied` 已经是数字**（不是数组），
  别写 `.length`（我写了，恒得到 `-1`，白排查一轮）。
- ProMax 在 768~1023（平板）下 `open()` 会因 `isDesktop()`（>1023）直接 return，
  文章面板打不开 —— **既有问题**（改前也这样），与本轮改动无关。

---

## 顶部搜索：把 `1.txt` 那份 iOS26 液态引擎整份搬过来（去掉黑条/壁纸/调参面板）

用户指令原文：「所有代码全搬过去 但是去掉黑条和壁纸和调参（调参用ctrl shift e)」。
参考实现 `E:\NanLuo321.github.io-main\1.txt`（iOS 26 QuartzCore `default.metallib`
反汇编复刻的 WebGL2 + SDF 玻璃）。移植落点：`src/utils/metaball-melt.ts`（新建，引擎）
+ `src/components/controls/TopSearch.svelte`（换成真引擎，删掉旧的 CSS+SVG goo 近似）。

### 根因：画布一片空白 —— 顶点着色器被我写歪了（本轮最大的坑）
移植时把全屏三角形写成了 `vec2((gl_VertexID<<1)&2, gl_VertexID&1)`，第二项应该是 `&2`：

| id | `(id<<1)&2` | `&1`（错） | `&2`（对） |
|---|---|---|---|
| 0 | 0 | (0,0) | (0,0) |
| 1 | 2 | (2,1) | (2,0) |
| 2 | 0 | (0,0) ← 与 id0 重合 | (0,2) |

→ 三角形**面积为零**，光栅化直接丢弃，`gl.drawArrays` 一个像素都不画，
而且 `gl.getError()` 恒为 0、上下文不丢、控制台无报错。修完立刻出图。
**教训：GLSL 里「不报错但什么都不画」优先怀疑顶点覆盖退化。**

### 排查这条路上两个浪费时间的假象
1. **`gl.readPixels` 读不到内容**（默认 `preserveDrawingBuffer:false`，且页面侧 rAF 与
   组件 rAF 谁先谁后不定）—— 同一位置一次 `3,4,6,251`、一次 `0,0,0,0`，
   连采 40 帧取最大也没救回来，被我误读成「shader 根本没画」。正解：截图 + 离线解 PNG。
2. **版本门 `#version-gate` 在 prep 等待期间才盖上**，截出一张全黑的图，
   差点被当成「改动把页面弄黑了」。prep 收尾要循环压几次。

### 「去掉黑条」的连带处理（都是量出来的，不是看出来的）
参考实现的黑条是 `uEdge=26` 的半平面。把它退到视口外只是第一步，
它还会留下两样东西，都实测到了：

1. **页面顶端一条全宽软阴影带**。`shade = exp(-(p.y-uEdge)/30)*0.26`，
   在纯灰 rig 上实测 y=0→alpha 0.31、y=26→0.11、y≈110 才消失，**逐点吻合模型**。
   黑条没了却留着它的影子。修法：阴影拆两份，`barVis = clamp(uEdge/26,0,1)`
   控制黑条那份，液滴自己那份用 `blob.x` 单独算（`max(shadeBar*barVis, shadeBlob)`）。
   液滴自带晕圈保持原样（实测 x=660/638/616/594 → 104/116/122/125，
   与 `exp(-blob.x/30)*0.26` 的 0.187/0.089/0.043/0.021 逐点吻合）。
2. **最顶一行一条 1px 全宽深色描边**：半平面下沿正好压在 y=0。
   修法：`meltTuning.edge` 从 `0` 改成 `-1`（下沿推出视口 1px），
   液颈几何不受影响。修完 `col 1200` 从 y=0 起全是 128。

   ⚠ `edge` 没暴露在调参面板里，但 `resetTune()` **遍历 `TUNE_DEFAULTS` 的全部键写回**，
   所以 `TopSearch.svelte` 里那份 `TUNE_DEFAULTS.edge` 必须同步改成 `-1`，
   否则一按「复位」描边就回来。

### 架构决定（沿用上一轮的结论）
canvas 的透明像素**不遮罩** `backdrop-filter`（整块都糊）→ 真折射只能由 DOM 层
`.ts-lens`（`border-radius` 裁剪 + `backdrop-filter`）承担，shader 只负责材质；
画布 `clearColor(0,0,0,0)` + `premultipliedAlpha:true` 让页面从底下透出。

### 验证结论（都是实测，非推断）
- idle：页面顶部干净，无黑条、无残留（真壁纸 + 纯灰 rig 两种底都看过）。
- 拖到 y≈70：液颈形态正确（顶端从 y=0 垂下、收细、末端鼓成液滴），左右无暗带。
- 拖到 y≈280：黑色液滴 —— **这是对的**，`sp.dark.x=1` 时本体就是贴边黑玻璃，
  只有落定成胶囊才 `dark.t=0` 变清玻璃（参考实现原样）。
- 落定→胶囊→搜索结果：正常（玻璃胶囊 + 真实折射 + 结果面板）。
- 岛内放大镜 `open({from})` 飞升：正常落位（能看到窗外天际线被折射）。
- 关闭 → `ts-retract` → 液滴缩回顶边，className 回到无 `liquid-on`。
- 调参面板：默认 `#top-search .ts-tune` **0 个**（不渲染），按 Ctrl+Shift+E 后 1 个 / 9 档
  滑块，拖 k→120 生效。面板在右上角 244×441。
- normal / lite：走原顶部导航栏，无画布、无胶囊、不串味。
- `tsc --noEmit` exit 0；两个改动文件 biome format 已过；`check-comments.cjs` 干净。
- SSR 产物（`fetch('/')`）含 `ts-melt`/`ts-lens`/`ts-blob`/`ts-grab`。
  注意 `{#if tuneOn}` 关着的块，类名仍会出现在**内联 CSS** 里，
  别拿 `html.includes("ts-tune")` 当「渲染了」的证据。

### 未完成 / 阻塞
- **`pnpm build` 跑不完，但与本次改动无关**：Vite 因 config 哈希变了
  （LQIP 脚本往 `src/constants/lqips.json` 写新条目）要清 `node_modules/.vite/deps`（102 文件），
  被 CLI safe-delete 批量删除守卫（阈值 50）拦下 → exit 1。
  另外 `generate-lqips.ts` 对 `public/w10/img/icon/win/shortcut.png` 一直报
  `unsupported image format`（存量问题）。要跑生产构建得在沙箱外执行。
- 兜底路径（无 WebGL2 → `paintFallback` 黑圆点）只做了代码审查，没在无头里造出来。

### 已清理
删掉会随站发布的 `public/_melt-test.html`、`public/_ts-probe.html`。

### skill 更新
`~/.workbuddy/skills/firefly-ui-visual-verify`：
新增 `scripts/pngscan.cjs`（离线解 PNG 数像素）、「量像素：别在页面里读 canvas」、
「用 rig 页给观感垫一块已知底」两节；补齐版本门二次出场、`pnpm build` 沙箱阻塞、
`#top-search` className 状态机 + `window.__topSearch` 入口这几个坑。

## 第二轮：胶囊常驻 + 悬停实时液态玻璃（2026-09-25 上午）
用户三诉求：胶囊默认可见 / 悬停才渲染液态玻璃且要实时 / 观感对齐参考实现。
- `metaball-melt.ts`：新增 hold 模式（与 phase 正交，**不能拨 phase**，否则 onPhase 把组件状态带偏）；
  shader 加 `uBlob2/uValid2/uK2`（悬停液凸，同一 rounded-box 原语再 union 一次）；
  holdOff 后液凸收干再 sleep；morph 落定遇 hold 不 sleep（hold 在 onSettle 里接班）。
- `TopSearch.svelte`：胶囊模板常挂 `show`（SSR 即可见）；`glass-on` 态引擎接管玻璃
  （DOM 底透掉）；`enterOpen` 幂等（重复进入不清 keyword）；拖拽/飞行时 `capVeil` 让位；
  close 不再退场，回默认档。
- 关键接线：胶囊平时挂着站内 `LiquidGlass.astro` 的内联 SVG 玻璃（`url(#lg-N)` + !important），
  hold 前必须 `__liquidGlass.unregister`、收摊后 `register` 还回去 —— register 在收尾
  几百 ms 的过渡窗口会 ret=false，`restoreLg` 按帧重试（≤12 帧）到 `data-lg-on` 挂上为止。
- 观感：默认档给 `.ts-capsule` 补了 `backdrop-filter: blur(18px)`（此前无模糊，壁纸原样
  透上来 = 用户截图里的浑浊色斑）；LiquidGlass 胶囊预设 blur 1.6→10 / tint 0.5→0.62（烟灰感）；
  引擎 refract 14→40 / cont 0.5→0.72（与 TUNE_DEFAULTS 两处必须同步）。
- 教训：本轮 Edit 工具多次「报成功但落盘丢失」（uniform 声明、U 表、模板各丢过一次），
  批量编辑后必须 grep 复核再继续。

## 第三轮：胶囊改回「默认隐藏，拖拽落定才出现」（2026-09-25 上午）
用户问「胶囊为什么每次打开页面都停留在网页上」→ 根因是我上一轮把「岛默认不隐藏」
理解成了这枚胶囊，模板硬编码 `class="ts-capsule show"`（TopSearch.svelte:828 附近）。
经 AskUserQuestion 确认：用户要的是**打开页面时胶囊不存在，拖拽/岛内起飞落定后才浮现**。
- 模板：`class="ts-capsule show"` → `class="ts-capsule" class:show={capsuleOn}`（capsuleOn 本来就在，
  被硬编码架空了）。
- `close()` 恢复退场：撤 show + 挂 is-out 淡出，melt 存在时走 `retractFromCapsule()` 液滴缩回顶边；
  兜底路径 paintFallback + phase=idle。
- `capGlassOff(restore = true)`：关闭时传 false，不把 LiquidGlass 的 SVG 玻璃还到一枚正在淡出的
  胶囊上（会中途换底色）；onPhase idle 时的 restoreLg 仍保留（拖拽回弹路径需要）。
- 探针五态实测：fresh op=0/pe=none → drag is-veil → settled show op=1/pe=auto → closing
  is-out+ts-retract → closed op=0/canvas 0，页面零 JS 错误。tsc/biome 干净。
- 教训：同一功能两轮需求方向相反（先"默认不隐藏"后"拖拽才出现"），确认对象+确认期望行为
  要一次问清，省一整轮返工。
